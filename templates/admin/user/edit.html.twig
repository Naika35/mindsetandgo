{% extends 'admin/base.html.twig' %}
{% block title %}Modifier les informations d'un utilisateur
{% endblock %}
{% block main %}

<h2>Modifier les informations de {{user.pseudo}}</h2>

	<div class="container">
		<div class="row">
			{{form_start(form)}}
            {{form_widget(form)}}
            <button class='btn btn-success'>Valider</button>
            {{form_end(form)}}
		</div>
	</div>

{% endblock %}
