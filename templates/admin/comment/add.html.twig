{% extends 'admin/base.html.twig' %}
{% block title %}Ajouter une commentaire
{% endblock %}
{% block main %}

<h1>Ajouter une commentaire</h1>

	<div class="container">
		<div class="row">
			{{form_start(form)}}
            {{form_widget(form)}}
            <button class='btn btn-success'>Valider</button>
            {{form_end(form)}}
		</div>
	</div>

{% endblock %}
