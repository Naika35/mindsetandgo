{% extends 'admin/base.html.twig' %}
{% block title %}Ajouter une citation
{% endblock %}
{% block main %}

<h1>Ajouter une citation</h1>

	<div class="container">
		<div class="row col-8">
			{{form_start(form)}}
            {{form_widget(form)}}
            <button class='btn btn-success'>Valider</button>
            {{form_end(form)}}
		</div>
	</div>

{% endblock %}
